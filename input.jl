### Function to integrate

function f(x)

    return x^2

end

start = 0                           # lower bound of the interval
end_ = 20                            # upper bound of the interval
num_step = 1000
